/** Default backend URL used for local Better Auth callbacks. */
export const DEFAULT_BACKEND_URL = "http://localhost:4111";

/** Default add-in origin trusted during local Office dialog flows. */
export const DEFAULT_ADDON_ORIGIN = "https://localhost:3000";
